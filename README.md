# Складской учет для продавцов Telegram бот

Бот для учета товаров, заявок на поставку и финансовых расчетов с продавцами.

## Функционал

### Для продавцов:
- 📦 **Заявка на поставку** - заказ товара
- 📤 **Отгруженные поставки** - отслеживание заявок
- 💰 **Реализовано** - учет продаж и передача денег админу
- 📊 **Остатки** - просмотр текущего склада и долга

### Для администратора:
- 📦 Управление поставками (подтверждение отгрузки)
- 💰 Управление платежами (подтверждение получения денег)
- 📊 Отчеты по всем продавцам
- ⚙️ Настройка товаров и цен
- 👥 Управление продавцами
- 🔐 Автоматические бэкапы при каждом действии

## Установка и запуск

### Локально
1. Клонировать репозиторий
2. Создать виртуальное окружение
3. Установить зависимости: `pip install -r requirements.txt`
4. Создать файл `.env` на основе `.env.example`
5. Запустить: `python main.py`

### На Render.com
1. Подключить репозиторий к Render
2. Создать Disk для постоянного хранения (путь `/data`)
3. Установить переменные окружения
4. Запустить

## Переменные окружения

| Переменная | Описание |
|------------|----------|
| BOT_TOKEN | Токен Telegram бота |
| ADMIN_IDS | ID администраторов через запятую |
| DATABASE_PATH | Путь к файлу БД |
| DEBUG | Режим отладки |

## Команды

- `/start` - Начало работы
- `/menu` - Главное меню
- `/backup` - Создать ручной бэкап (админ)
- `/restore` - Восстановить из бэкапа (админ)

## Бэкапы

При каждом действии продавца или админа автоматически создается JSON-бэкап и отправляется администратору в личные сообщения.
